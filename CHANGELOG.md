# 更新日志 (Changelog)

本项目所有值得记录的变更都会列在此文件。格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循语义化版本。

## [1.3.26] - 2026-09-12

### 变更
- **「总曲目数」成为可勾选字段（默认不勾）**：此前它不在字段清单里，数据源给出值时会被**无条件**
  写进文件（设置页里看不到、也关不掉）。现在与其它字段完全一致 —— 勾选才写、未勾选时手动写入会被
  拦下，并会出现在手动刮削窗口的字段对照里。
- mp3 / ape 的总曲目与曲目号在标签里是合并存放的（`7/12`）：当候选只给出总数、没有曲目号时，
  写入会沿用文件里已有的曲目号凑成 `N/总数`，避免总数被静默丢掉。

### 清理
- 删除没人使用的旧字段开关表 `FIELD_SWITCH`（字段生效与否统一由 `active_fields` 决定）。

## [1.3.25] - 2026-09-12

### 变更
- **「已人工」只由人工逐条操作产生**：只有「队列里手动指定为已人工」和「手动刮削窗口逐条写字段」
  会打上「已人工」标签并写入永久记忆（重新扫描也不会再刮它）。批量处理、补写写入的文件记为
  「自动写入」，不产生永久记忆。
- **批量「按最高分写入」需要先启用写入**：未启用时直接拒绝并说明原因，不再出现
  「队列被清空、音乐文件一个字没改」的假完成。学习模式下的行为就是「只预演、不落盘」。
- **状态只认当前生效的那一份**：任务状态改变时清掉该文件的历史选择/跳过记录；「补写」改按
  「当前是否已有写入记录」判断，不再被旧记录静默跳过（返回里同时给出写入/跳过数）。
- **改状态不再清空已匹配信息**：补写、刮削中途把任务放回队列时，保留任务行里已匹配的
  歌名、歌手、专辑、年份与得分（此前会被清成空）。
- **「开始刮削」如实反馈**：「开始刮削 / 只刮待处理 / 重新刮人工队列」都会返回没启动的具体原因
  （没有可用的数据源插件、已有刮削在运行、没有待处理的文件），界面照实提示，不再一律弹「已启动」。
- **筛选结果即时更新**：扫描目录或清空任务后立刻刷新「缺歌词 / 缺封面」等筛选缓存，
  新扫入的歌不必再等最长 10 分钟。
- **MP3 / APE 的曲目号读回与 FLAC 一致**：磁盘上的「7/12」读回为「曲目号 7 + 总曲目 12」。
- **文件操作统一鉴权**：播放、内嵌封面、手动写字段、手动刮削、重命名、删除统一限定在
  「已配置的音乐目录」内；未配置音乐目录时直接提示（此前部分接口没有这道闸）。

### 修复
- 非 QQ 数据源的搜索缓存键改用「实际发出去的查询词」：正式版与 Live/伴奏版等不同查询词
  不再共用同一份缓存结果（此前会互相串味，正式版可能拿到 Live 版的结果）。
- 清理：去掉只写不读的空闲时间戳变量、已移除功能的过期注释，以及无人使用且未做编码的音频地址字段。

## [1.3.24] - 2026-09-12

### 变更
- **代码清理（无功能变化）**：删除无人引用的 `scheduler.is_running()`、`writer.has_metadata()`、
  `filenames.reverse_candidates()`，以及多余的 import（`typing.Optional`、`run_server` 的 `threading`、
  人工记录导出接口里的空导入）和前端一条已无对应元素的 `.cm-body` 样式；
  旧链路的 `parse_candidates()` 等仍被「缓存导出」使用的函数保持不动。
- 插件来源说明由本机 NAS 路径改为仓库 `data-source-plugins/` 目录（对任何用户都成立）。

## [1.3.23] - 2026-09-12

### 修复
- **「已人工」不再是易失状态**：此前清空任务列表后重新扫描，已人工处理过的歌曲会被重新排进「待处理」，
  要等刮削跑一遍才会变成「跳过」。现在**重新扫描时直接标为「已人工」**，不会再被打回待处理、
  也不会再次被刮削；只有人工把它指定成别的状态，它才会离开「已人工」。
- **刮削命中永久记忆时不再写成「跳过」**：改为就地标回「已人工」（备注说明原因），
  与人工指定「已人工」保持同一个状态语义。
- **文件被改名/搬家也能认出**：永久记忆新增 `file_size`，按「大小 + 内容哈希」兜底识别，
  外部整理过目录的歌曲重新扫描后依然自动标为「已人工」（大小不符就不读文件内容，
  整库扫描只是每文件一次 stat，不会把全库哈希一遍）。
- **导入人工记录后立即生效**：导入记录时把队列里同路径的「待处理」任务同步纠正为「已人工」，
  结果里返回同步条数并在界面提示。

### 变更
- **「删除任务」不再抹掉人工记录**：删除只表示「把这首歌移出队列」，永久记忆保留，
  重新扫描后仍标为「已人工」。要让某首歌重新刮削，请先把它的状态手动指定为「待处理」
  （这一步会清掉永久记忆）。删除确认框里也写明了这一点。

## [1.3.22] - 2026-09-11

### 新增
- 设置页新增「**人工处理记录**」导出 / 导入：
  - 「⬇ 导出到本机」把永久记忆与人工选择记录导出成 JSON，直接下载到**当前终端**
    （电脑浏览器 / 手机 / iOS / APP 内均可），文件名形如 `music-meta-manual-20260911-224824.json`；
  - 「⬆ 导入记录文件」选择导出文件即可合并进本机记录，**按文件路径合并、不会删除本机已有记录**
    （重复导入是幂等的：已存在的记为“更新”）；
  - 设置页同时显示当前条数（人工记录 N 条 · 选择记录 M 条）。

### 移除
- 移除 1.3.21 引入的「合并缓存库」功能（`/api/cache/merge` 接口、`cache.merge_from`、
  目录设置里的入口）。缓存库合并改用官方通路在外部完成：
  `file.download`（fnos-ws 技能）取签名 URL 下载 → 本地合并 → 覆盖上传回缓存目录。

## [1.3.21] - 2026-09-11

### 新增
- 「目录设置」新增**缓存合并**：可以把旧缓存目录的 musicmeta_cache.db 合并进当前缓存库，
  换过缓存目录之后不必重新联网抓一遍。同一个 key 保留 expire 更晚（也就是较新）的那份，
  不会把当前缓存改旧；源库只读，不会被改动。

## [1.3.20] - 2026-09-11

### 修复
- **候选下拉菜单打开时是一根巨大的单列**：`.pop.open{display:block}`（两个类）压过了
  `.cand-menu{display:grid}`（一个类），网格失效；第二下点击触发重排后才偶然恢复多列。
  现在容器只负责定位与外观，多列网格移到内层 `.cm-grid`，不再有优先级冲突；
  菜单宽度也从 980px 收到 760px（够 3～4 列），高度上限 44vh。
- **菜单不跟随「手动刮削」按钮**：菜单过宽时被夹到窗口左侧。现在改为「先与按钮左对齐，
  右边放不下则与按钮右对齐」，下方空间不足且上方够时翻到按钮上方。
- **菜单关不掉**：菜单顶部加了标题栏与「✕ 关闭」按钮；点菜单空白区域也会关闭；
  Esc 依然可关（再点一次同一个「手动刮削」按钮也能收起）。
- **同步歌词高亮对但不居中**：`.lrc-box` 是 static 定位，子元素 `offsetTop` 相对的是更外层
  的定位祖先，算出来的滚动位置是错的（NaN/越界）。改用 `getBoundingClientRect()` 计算
  相对偏移，并给歌词框加 `position:relative`，现在当前句会真正滚到歌词框正中。

## [1.3.19] - 2026-09-11

### 变更
- 候选下拉菜单在窄屏（手机/窄窗口）下的最小列宽由 196px 调至 146px：
  360px 宽的窗口也能排下 2 列（此前只能 1 列），列数不再被挤成单列。

## [1.3.18] - 2026-09-11

### 修复
- 手动刮削窗口的同步歌词现在会真正跟着播放走：
  - **打开窗口即自动开始播放**（若被浏览器拦截自动播放，会提示点一下播放按钮）。
  - 当前句高亮并**自动滚动居中**；拖动进度、暂停、跳转后立即对齐。
  - **用户手动滚动歌词即取消自动居中**，此后完全按用户的滚动位置显示；
    点任意歌词行可恢复跟随（提示文案同步刷新）。
  - 歌词框上下留白，保证第一句与最后一句也能滚到中间。

### 新增
- 内嵌歌词没有时间轴时，可改用**候选歌词同步**：选完候选歌词后播放器旁出现
  「用候选歌词同步 / 切回内嵌歌词」按钮；若内嵌歌词无时间轴而候选歌词有时间轴，会自动切换。

## [1.3.17] - 2026-09-11

### 变更
- 手动刮削窗口的候选下拉菜单精简为「**字段内容 + 数据源**」：不再显示网址、歌名、
  歌手等其它信息；封面图字段显示 40px 缩略图 + 来源（网址只作为图片地址，不再当文本展示）。
- 候选下拉改为**多列自适应网格**（列宽约 196px，随窗口宽度自动增减列数），
  勾选多个数据源、每个源候选较多时也能一眼看全，不用来回滚动。
- 歌词候选在菜单里显示「**歌词首句 + 数据源**」：插件自带歌词的直接显示；
  没带的按需后台补齐首句（走应用层缓存，取过一次不再请求），点选后仍载入完整歌词。

## [1.3.16] - 2026-09-11

### 新增
- 设置页新增「风控设置」弹窗（原请求间隔那一行收进来），共 8 项、每项都写明作用：
  请求间隔、并发数、每源候选数、请求超时、失败重试次数、单次运行上限、
  每处理 N 首暂停一次、暂停时长。其中「单次运行上限」和「每 N 首暂停」是应用层控制，
  对所有数据源都生效（大批量刮削时能显著降低被风控的概率）。
- 设置页新增「目录设置」弹窗：音乐库目录（含递归开关）、数据源插件目录、
  **元数据缓存目录**（新配置 `cache_dir`：换目录=清空缓存重新联网抓取），
  并展示缓存实际落盘位置与应用数据目录。
- 安装向导新增「元数据缓存目录」配置项（`wizard_cache_dir`，可留空=应用数据目录），
  经 `cmd/main` 转发为 `MMW_CACHE_DIR`，首次启动写入配置。
- 右上角设置入口改为矢量齿轮图标 + 「设置」二字（悬停有轻微旋转反馈）。

### 变更
- 设置页把「请求间隔/并发数/每源候选数」与「音乐目录/插件目录」两组散行
  收敛成「风控设置」「目录设置」两个按钮，界面更整齐。
- 手动刮削窗口里「原有内嵌封面」改成 40px 缩略图（此前缺少尺寸规则，会按原图尺寸铺开）。

### 移除
- 「空闲自动退出」整体移除：配置项、后台看门狗线程与界面开关一并删除，后台进程保持常驻。

## [1.3.15] - 2026-09-11

### 修复
- 状态文案统一：状态过滤器与卡片标签的「待人工关」改为「待人工」。
- 顶栏不再重复计数（计数已在状态过滤器上）：总数与进度合并到队列页进度条旁，
  显示为「进度 62%（已处理 1723 / 共 2767）」。
- 修「下拉菜单还没点就消失」：手动刮削窗口的内嵌歌词会随播放自动滚动，
  而浮层原先监听捕获阶段的 scroll 事件并关闭自己；现在只在「点击外部 / Esc /
  窗口尺寸变化」时关闭。
- 手动刮削窗口的封面候选下拉改为规整列表：每行一个小缩略图 + 来源 + 歌名-歌手 + URL，
  替换原来直接铺大图的做法；选中候选后候选框旁也会显示该缩略图。

### 新增
- 手动刮削窗口的封面图行直接显示**文件内嵌封面的缩略图**（新增 `/api/embedded-cover`
  端点，只允许读取已授权音乐目录内的文件）。
- 设置页「元数据源」改为下拉菜单（与「生效字段」同一套交互），下方显示「已选：…」。

## [1.3.14] - 2026-09-11

### 变更（对齐官方 fpk 打包规范）
- **入口图标按官方规范命名**：`app/ui/images/` 提供 `icon_64.png`（64×64）与
  `icon_256.png`（256×256），`app/ui/config` 的 `icon` 改为官方推荐的
  `images/icon_{0}.png`（由系统按所需尺寸替换 `{0}`）。此前只放了单个 256px 文件。
- **manifest 补齐规范字段**：`maintainer_url` 原来为空，现填仓库地址（官方要求
  维护者网站或联系方式）。
- **manifest.changelog 自动生成**：打包时从 CHANGELOG.md 当前版本段落提取要点，
  写入包内 manifest 的 `changelog` 字段（应用中心「更新说明」用它展示），
  仓库里的 manifest 保持干净、不写死版本内容。

### 变更（发版流程可核验）
- 打包脚本新增**校验值存档**：`版本存档/<版本>/checksums.txt` 记录 fpk 的 sha256、
  包内 app.tgz 的 md5、manifest.checksum 及其自洽性；`payload.md5` 记录解包后
  每个文件的 md5，可用 `md5sum -c` 事后核验。
- 一键发版脚本新增两步校验：① CI 产物载荷与本地构建**逐文件比对**（不一致则中止升级）；
  ② 升级后比对**运行中前端与发布包内前端**的 md5，确认部署的就是发布的那份。

## [1.3.13] - 2026-09-11

### 变更（界面重构：设置入口 + 生效字段 + 单队列 + 手动刮削窗口）
- **设置入口改为右上角齿轮**：全局显示，点击进入设置页（原「配置」标签页入口取消）。
- **新增「生效字段」**（设置页下拉勾选，默认只勾 歌名/歌手/封面图/歌词）：
  未勾选的字段在应用里**任何界面任何位置都不显示**，也**不会被写入文件**；
  队列卡片、手动刮削窗口、元数据筛选条件全部按生效字段动态生成，
  写入接口也一并拦截未生效字段（防止绕过界面写入）。
  旧的逐个字段 `write_*` 开关与已废弃的 `threshold` 配置项已从代码与数据库中移除。
- **队列页合并**：原「总数/自动写入/待人工/已人工/跳过/错误」六个列表页取消，
  合并为一个队列，顶部改为状态标签过滤器（全部·自动写入·待刮削前·已人工·待人工关·
  跳过·错误，各带数量）；「自动写入」档同时包含正在写入（processing）的任务。
- **卡片精简**：去掉得分列/已写入列/匹配结果列；卡片为 文件名 + 状态标签 + 操作列
  （重命名 / 手动刮削 / 删除）。重命名为跟随按钮的小浮层（单行输入 + 确认/取消）。
- **状态标签可点击指定**：弹出 6 种状态的下拉菜单，每行右侧一个蓝色「指定」按钮
  （固定列宽、竖向对齐），当前状态的「指定」置灰不可点；指定为「已人工」会写入永久
  记忆（后续刮削自动排除），离开该状态则自动清除记忆。
- **手动刮削窗口**（居中自适应）：打开时即按生效数据源现场刮削并缓存候选
  （每个源取「每源候选数」条）；上方是本地音频播放器 + 内嵌歌词同步高亮播放器
  （点歌词行可跳转），下方按生效字段逐行展示「原有 / 候选 / 手动刮削 / 写入」；
  封面图行下方是「原有内嵌歌词 vs 候选歌词」对比框（自适应高度）。
  候选框默认空白，点「手动刮削」下拉只列该字段的候选值，点选后填入候选框，
  写入按钮仅在候选框有内容时可用（有内容才覆盖写入）。

### 移除（无用代码清理）
- 后端接口从 45 个精简到 26 个：删除 /api/activity、/api/pending、/api/mark-done、
  /api/tasks/restore、/api/tasks/export、/api/filter-counts、/api/decide、
  /api/refresh、/api/manual-search、/api/lyrics/{songmid}、/api/song-detail、
  /api/field-candidates、/api/manual-done(+remove)、/api/recognize、/api/cover-upload、
  /api/reprocess-errors 等（功能已由状态过滤器、/api/task-status、
  /api/scrub、/api/cand-lyrics、/api/field-write 覆盖）。
- 数据层删除 6 个无引用函数（next_pending / list_manual_done / count_manual_done /
  manual_done_hashes / manual_done_paths / restore_manual_done）、过滤计数缓存；
  一并清理未使用的 fastapi 导入与前端死代码（旧列表/详情弹窗 JS、无用 CSS）。
- 前端新增 `musicmeta/fields.py`：字段定义的单一来源（key/显示名/标签键/候选属性），
  webapp 与 scheduler 共用，避免字段清单散落多处。

## [1.3.12] - 2026-09-11

### 变更（刮削匹配规则重写：候选词搜索 + 搜索结果反推）
- **不再从文件名猜「哪个是歌手、哪个是歌名」**：改为把文件名拆成候选关键词直接搜索，
  再用搜索结果反推歌名与歌手——搜索本身就是解析手段。
- **第 1 步 清洗文件名**：去扩展名、去音轨号（`01.` / `01 -` / `Track01`，必须带分隔符，
  避免把「2002 年的第一场雪」误判为音轨号）、去噪音标签（`[320K]` / `(Hi-Res)` / `官方版` /
  `MV` / `无损` 等）；`(Live)` / `(Remix)` / `(Acoustic)` 等版本标记搜索前移除，
  但因为校验用的是原始文件名，这些词仍允许出现在搜索结果里。
- **第 2 步 候选查询词**：整串 + 按分隔符优先级（` - ` > `–` > `—` > `-` > `_` > `|`）
  切出的左段/右段 + 书名号/括号内内容。
- **第 3 步 命中即停**：某个候选词一旦出现通过校验的结果，就不再发后续候选词；
  `歌手-歌名` / `歌名-歌手` 顺序无关，因此删除了原来的「反序候选补搜第二轮」。
  典型文件从「最多 6 个候选 × N 源」降到「1 个候选 × N 源」。
- **第 4 步 反推校验（新的命中判定）**：结果的 trackName 与 artistName 都必须出现在
  原始文件名中（不区分大小写、忽略标点/空格/全半角差异）。多歌手结果（如
  `周杰伦/费玉清`）允许其中一位歌手命中。
- **打分降级为排序辅助**：是否自动写入不再看阈值，改由反推校验决定；排序分改为
  0~100 的客观信号（校验通过 +40、时长接近度最高 +40、歌名/歌手与候选词吻合 +15/+8/+5），
  修复了此前用「候选词当期望歌名」打分导致的负分问题。
- **配置页「匹配阈值」已废弃**（置灰保留，旧库配置值不再参与判定）。
- **候选列表新增 `verified` 标记**（DB 自动迁移补列），待人工队列中通过校验的候选
  显示「反推✓」并排在最前。
- 文件名只有歌名、没有歌手时，反推校验天然无法满足，会给出明确提示
  （建议手动搜索或改用指纹模式），不会编造歌手。
- 插件写作者注意：文件名模式下插件收到的 `search(title, artist)` 中 `artist` 为空、
  `title` 是候选关键词（不再预判歌手/歌名），插件自身的 confidence 只作为参考，
  应用层会用文件名 + 时长重算排序分。

## [1.3.11] - 2026-09-09

### 修复
- 手动搜索输入框打字时输入指针自动跳走：根因同 1.3.10（轮询重建列表 DOM 替换了
  正在输入的输入框导致焦点丢失）。现在检测到焦点在手动搜索输入框内时跳过本次列表重绘，
  失焦后再刷新，输入过程中光标保持稳定。

## [1.3.10] - 2026-09-09

### 修复
- 待人工队列「手动搜索」面板"点开自己就没了"：页面每 5 秒轮询会重建列表 DOM，
  原先展开状态与输入内容会被重置为收起。现在展开状态与歌名/歌手输入按文件路径记忆，
  轮询重绘后自动恢复展开并保留已输入内容。

## [1.3.9] - 2026-09-09

### 变更
- 应用介绍（manifest desc）重新排版：由单段长文改为分节短条目（▍核心能力/多数据源插件/插件下载与使用/数据安全），
  加 <br/> 空行留白，删除冗余叙述，正文更专业易读（长度约 939→593 字符）。

## [1.3.8] - 2026-09-09

### 变更
- README 新增「数据源插件」章节：说明插件为单文件 .py、下载位置（本仓库 data-source-plugins/
  与 Release）、使用三步（复制到插件目录→重启→配置页勾选），并附插件清单与开源协作说明。

## [1.3.7] - 2026-09-09

### 变更
- 应用介绍（manifest desc）新增「数据源插件 · 下载与使用」段落：说明本安装包不内置数据源、
  插件为单文件 .py、GitHub 仓库 data-source-plugins 目录下载地址，以及 ①复制到插件目录
  ②重启应用 ③配置页勾选 三步使用步骤（含具体插件文件名与开源说明）。

## [1.3.6] - 2026-09-09

### 变更
- 捐赠区「作者自述（不会写代码、烧 token 请 AI 做）」改为醒目强调块：金黄渐变背景 + 橙色边框 +
  加大加粗重点词（💰 花钱烧 token 请 AI 帮忙），避免被用户一眼跳过。

## [1.3.5] - 2026-09-09

### 变更
- 配置页底部区块改版：「完全开源 · 欢迎一起来完善」GitHub 开源号召移到最上方并强化显示
  （渐变高亮块 + 强调链接按钮样式），置于捐赠区之上。
- 移除原先底部重复的两段感谢文案，合并为一句。

## [1.3.4] - 2026-09-09

### 变更
- 捐赠区块文案补充：作者自述「完全不会写应用/编译，全靠花钱烧 token 请 AI 完成」。
- 捐赠区块附上 GitHub 仓库地址（github.com/Tianxiaodudou/music-meta-web），邀请所有用户前往仓库完善、改进、提 issue / PR（GPL-3.0 开源）。

## [1.3.3] - 2026-09-09

### 新增
- 配置页底部新增「☕ 用爱发电 · 支持一下」区块：展示微信 / 支付宝收款码。
- 文案明确：应用完全免费、无广告、无内购、代码开源（GPL-3.0）；赞赏纯属<b>自由自愿</b>，金额随意，与任何功能/权限无关，仅用于支持开发者继续维护。

### 说明
- 收款码图片以 base64 内联进页面，不依赖外部文件与新增静态路由。

## [1.3.2] - 2026-09-09

### 新增
- 待人工队列新增「✎ 手动搜索」：文件名不是「歌名-歌手」格式或识别不对时，可直接输入正确的歌名/歌手跨源搜索，无需改文件名（后端新增 `POST /api/manual-search`）。
- 文件名解析自动识别更多分隔符：`_`、`–`(EN DASH)、`—`(EM DASH)、`｜`、`|` 等统一按 `-` 处理。
- 自动刮削与单文件「重新搜索」新增反序兜底：文件名若是「歌手-歌名」顺序（如 `周杰伦-晴天.flac`），正向解析结果不达标时会自动交换歌名/歌手补搜一轮。

### 修复
- 修复正序/反序两轮搜索命中同一首歌时，低分候选先占位导致高分被丢弃的问题（改为按「源+歌曲ID」保留最高分）。
- 修复单文件重新搜索时反序候选被二次交换回原顺序、导致兜底失效的问题。

### 变更
- README 精简为纯功能介绍；新增本 CHANGELOG。
- GitHub Actions 发布 Release 时自动把对应版本的更新日志写入 Release 正文。

## [1.3.0] - 2026-09-09

### 变更
- 仓库整体迁移到 GitHub（Tianxiaodudou/music-meta-web，GPL-3.0）。
- 应用图标替换为设计处理版（去除「豆包AI生成」水印，四角白区转透明圆角）。
- 数据源插件（qqmusic/netease/kugou/kuwo/lrclib/theaudiodb/itunes）单独归档到仓库 `data-source-plugins/` 目录，供分发使用；**插件不参与 fpk 打包**（本安装包始终不内置数据源，安装后从插件目录加载）。
- 新增 GitHub Actions 工作流：推送 `vX.Y.Z` tag 自动用官方 fnpack 在线编译并发布 `music-meta-web-<版本号>.fpk` 到 Releases。

### 说明
- 1.3.0 相对 1.2.1 的主要区别：图标更新、插件目录整理、仓库迁至 GitHub 并启用自动发布，功能代码与 1.2.1 相同。

## [1.2.1] - 2026-09-09

### 新增
- 应用图标更新为设计稿处理版（新图标）。

### 变更
- 打包流程固化：版本号每次递增、产物文件名带版本号、旧版 fpk 保留不删（本地脚本 `打包fpk.sh`）。

## [1.2.0] - 2026-09-02

### 变更
- 应用信息完善：maintainer=Tianxiaodudou、distributor=A鱼儿；应用介绍 desc 改为详细中文 HTML 版。
- 空闲自动退出默认关闭（原默认 30 分钟），改为配置页可选项。
- 总数页过滤加「正在过滤…」提示、结果缓存与写后自动失效。
- 「无歌词过滤→刮削歌词→用于此字段」交互修复（完整歌词不再内联进按钮，改用 song_id 取值）。
- 修复字段写入后未从过滤器消失的问题（写操作统一失效过滤缓存）。

## [1.1.0] - 2026-08-31

### 新增
- 多数据源插件化重构：QQ 音乐 / 网易云 / 酷狗 / 酷我 / LRC Lib / TheAudioDB / iTunes 单文件插件即插即用。
- 全局共享限速 + 本地缓存（SQLite）：同曲目零重复请求，缓存支持已确认歌曲一键导出。
- AcoustID 音频指纹识别模式（需自备 Key）。
- 目录级写保护、真实写入开关、中文错误提示。

## [1.0.0] - 2026-08-30

### 新增
- 首个可安装版本：按「歌曲名-歌手」文件名识别 → 多源检索 → 自动写入 / 人工辅助队列。
