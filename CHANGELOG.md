# 更新日志 (Changelog)

本项目所有值得记录的变更都会列在此文件。格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循语义化版本。

## [1.3.13] - 2026-09-11

### 变更（界面重构：设置入口 + 生效字段 + 单队列 + 手动刮削窗口）
- **设置入口改为右上角齿轮**：全局显示，点击进入设置页（原「配置」标签页入口取消）。
- **新增「生效字段」**（设置页下拉勾选，默认只勾 歌名/歌手/封面图/歌词）：
  未勾选的字段在应用里**任何界面任何位置都不显示**，也**不会被写入文件**；
  队列卡片、手动刮削窗口、元数据筛选条件全部按生效字段动态生成，
  写入接口也一并拦截未生效字段（防止绕过界面写入）。
  旧的逐个字段 `write_*` 开关与已废弃的 `threshold` 配置项已从代码与数据库中移除。
- **队列页合并**：原「总数/自动写入/待人工/已人工/跳过/错误」六个列表页取消，
  合并为一个队列，顶部改为状态标签过滤器（全部·自动写入·待刮削前·已人工·待人工关·
  跳过·错误，各带数量）；「自动写入」档同时包含正在写入（processing）的任务。
- **卡片精简**：去掉得分列/已写入列/匹配结果列；卡片为 文件名 + 状态标签 + 操作列
  （重命名 / 手动刮削 / 删除）。重命名为跟随按钮的小浮层（单行输入 + 确认/取消）。
- **状态标签可点击指定**：弹出 6 种状态的下拉菜单，每行右侧一个蓝色「指定」按钮
  （固定列宽、竖向对齐），当前状态的「指定」置灰不可点；指定为「已人工」会写入永久
  记忆（后续刮削自动排除），离开该状态则自动清除记忆。
- **手动刮削窗口**（居中自适应）：打开时即按生效数据源现场刮削并缓存候选
  （每个源取「每源候选数」条）；上方是本地音频播放器 + 内嵌歌词同步高亮播放器
  （点歌词行可跳转），下方按生效字段逐行展示「原有 / 候选 / 手动刮削 / 写入」；
  封面图行下方是「原有内嵌歌词 vs 候选歌词」对比框（自适应高度）。
  候选框默认空白，点「手动刮削」下拉只列该字段的候选值，点选后填入候选框，
  写入按钮仅在候选框有内容时可用（有内容才覆盖写入）。

### 移除（无用代码清理）
- 后端接口从 45 个精简到 26 个：删除 /api/activity、/api/pending、/api/mark-done、
  /api/tasks/restore、/api/tasks/export、/api/filter-counts、/api/decide、
  /api/refresh、/api/manual-search、/api/lyrics/{songmid}、/api/song-detail、
  /api/field-candidates、/api/manual-done(+remove)、/api/recognize、/api/cover-upload、
  /api/reprocess-errors 等（功能已由状态过滤器、/api/task-status、
  /api/scrub、/api/cand-lyrics、/api/field-write 覆盖）。
- 数据层删除 6 个无引用函数（next_pending / list_manual_done / count_manual_done /
  manual_done_hashes / manual_done_paths / restore_manual_done）、过滤计数缓存；
  一并清理未使用的 fastapi 导入与前端死代码（旧列表/详情弹窗 JS、无用 CSS）。
- 前端新增 `musicmeta/fields.py`：字段定义的单一来源（key/显示名/标签键/候选属性），
  webapp 与 scheduler 共用，避免字段清单散落多处。

## [1.3.12] - 2026-09-11

### 变更（刮削匹配规则重写：候选词搜索 + 搜索结果反推）
- **不再从文件名猜「哪个是歌手、哪个是歌名」**：改为把文件名拆成候选关键词直接搜索，
  再用搜索结果反推歌名与歌手——搜索本身就是解析手段。
- **第 1 步 清洗文件名**：去扩展名、去音轨号（`01.` / `01 -` / `Track01`，必须带分隔符，
  避免把「2002 年的第一场雪」误判为音轨号）、去噪音标签（`[320K]` / `(Hi-Res)` / `官方版` /
  `MV` / `无损` 等）；`(Live)` / `(Remix)` / `(Acoustic)` 等版本标记搜索前移除，
  但因为校验用的是原始文件名，这些词仍允许出现在搜索结果里。
- **第 2 步 候选查询词**：整串 + 按分隔符优先级（` - ` > `–` > `—` > `-` > `_` > `|`）
  切出的左段/右段 + 书名号/括号内内容。
- **第 3 步 命中即停**：某个候选词一旦出现通过校验的结果，就不再发后续候选词；
  `歌手-歌名` / `歌名-歌手` 顺序无关，因此删除了原来的「反序候选补搜第二轮」。
  典型文件从「最多 6 个候选 × N 源」降到「1 个候选 × N 源」。
- **第 4 步 反推校验（新的命中判定）**：结果的 trackName 与 artistName 都必须出现在
  原始文件名中（不区分大小写、忽略标点/空格/全半角差异）。多歌手结果（如
  `周杰伦/费玉清`）允许其中一位歌手命中。
- **打分降级为排序辅助**：是否自动写入不再看阈值，改由反推校验决定；排序分改为
  0~100 的客观信号（校验通过 +40、时长接近度最高 +40、歌名/歌手与候选词吻合 +15/+8/+5），
  修复了此前用「候选词当期望歌名」打分导致的负分问题。
- **配置页「匹配阈值」已废弃**（置灰保留，旧库配置值不再参与判定）。
- **候选列表新增 `verified` 标记**（DB 自动迁移补列），待人工队列中通过校验的候选
  显示「反推✓」并排在最前。
- 文件名只有歌名、没有歌手时，反推校验天然无法满足，会给出明确提示
  （建议手动搜索或改用指纹模式），不会编造歌手。
- 插件写作者注意：文件名模式下插件收到的 `search(title, artist)` 中 `artist` 为空、
  `title` 是候选关键词（不再预判歌手/歌名），插件自身的 confidence 只作为参考，
  应用层会用文件名 + 时长重算排序分。

## [1.3.11] - 2026-09-09

### 修复
- 手动搜索输入框打字时输入指针自动跳走：根因同 1.3.10（轮询重建列表 DOM 替换了
  正在输入的输入框导致焦点丢失）。现在检测到焦点在手动搜索输入框内时跳过本次列表重绘，
  失焦后再刷新，输入过程中光标保持稳定。

## [1.3.10] - 2026-09-09

### 修复
- 待人工队列「手动搜索」面板"点开自己就没了"：页面每 5 秒轮询会重建列表 DOM，
  原先展开状态与输入内容会被重置为收起。现在展开状态与歌名/歌手输入按文件路径记忆，
  轮询重绘后自动恢复展开并保留已输入内容。

## [1.3.9] - 2026-09-09

### 变更
- 应用介绍（manifest desc）重新排版：由单段长文改为分节短条目（▍核心能力/多数据源插件/插件下载与使用/数据安全），
  加 <br/> 空行留白，删除冗余叙述，正文更专业易读（长度约 939→593 字符）。

## [1.3.8] - 2026-09-09

### 变更
- README 新增「数据源插件」章节：说明插件为单文件 .py、下载位置（本仓库 data-source-plugins/
  与 Release）、使用三步（复制到插件目录→重启→配置页勾选），并附插件清单与开源协作说明。

## [1.3.7] - 2026-09-09

### 变更
- 应用介绍（manifest desc）新增「数据源插件 · 下载与使用」段落：说明本安装包不内置数据源、
  插件为单文件 .py、GitHub 仓库 data-source-plugins 目录下载地址，以及 ①复制到插件目录
  ②重启应用 ③配置页勾选 三步使用步骤（含具体插件文件名与开源说明）。

## [1.3.6] - 2026-09-09

### 变更
- 捐赠区「作者自述（不会写代码、烧 token 请 AI 做）」改为醒目强调块：金黄渐变背景 + 橙色边框 +
  加大加粗重点词（💰 花钱烧 token 请 AI 帮忙），避免被用户一眼跳过。

## [1.3.5] - 2026-09-09

### 变更
- 配置页底部区块改版：「完全开源 · 欢迎一起来完善」GitHub 开源号召移到最上方并强化显示
  （渐变高亮块 + 强调链接按钮样式），置于捐赠区之上。
- 移除原先底部重复的两段感谢文案，合并为一句。

## [1.3.4] - 2026-09-09

### 变更
- 捐赠区块文案补充：作者自述「完全不会写应用/编译，全靠花钱烧 token 请 AI 完成」。
- 捐赠区块附上 GitHub 仓库地址（github.com/Tianxiaodudou/music-meta-web），邀请所有用户前往仓库完善、改进、提 issue / PR（GPL-3.0 开源）。

## [1.3.3] - 2026-09-09

### 新增
- 配置页底部新增「☕ 用爱发电 · 支持一下」区块：展示微信 / 支付宝收款码。
- 文案明确：应用完全免费、无广告、无内购、代码开源（GPL-3.0）；赞赏纯属<b>自由自愿</b>，金额随意，与任何功能/权限无关，仅用于支持开发者继续维护。

### 说明
- 收款码图片以 base64 内联进页面，不依赖外部文件与新增静态路由。

## [1.3.2] - 2026-09-09

### 新增
- 待人工队列新增「✎ 手动搜索」：文件名不是「歌名-歌手」格式或识别不对时，可直接输入正确的歌名/歌手跨源搜索，无需改文件名（后端新增 `POST /api/manual-search`）。
- 文件名解析自动识别更多分隔符：`_`、`–`(EN DASH)、`—`(EM DASH)、`｜`、`|` 等统一按 `-` 处理。
- 自动刮削与单文件「重新搜索」新增反序兜底：文件名若是「歌手-歌名」顺序（如 `周杰伦-晴天.flac`），正向解析结果不达标时会自动交换歌名/歌手补搜一轮。

### 修复
- 修复正序/反序两轮搜索命中同一首歌时，低分候选先占位导致高分被丢弃的问题（改为按「源+歌曲ID」保留最高分）。
- 修复单文件重新搜索时反序候选被二次交换回原顺序、导致兜底失效的问题。

### 变更
- README 精简为纯功能介绍；新增本 CHANGELOG。
- GitHub Actions 发布 Release 时自动把对应版本的更新日志写入 Release 正文。

## [1.3.0] - 2026-09-09

### 变更
- 仓库整体迁移到 GitHub（Tianxiaodudou/music-meta-web，GPL-3.0）。
- 应用图标替换为设计处理版（去除「豆包AI生成」水印，四角白区转透明圆角）。
- 数据源插件（qqmusic/netease/kugou/kuwo/lrclib/theaudiodb/itunes）单独归档到仓库 `data-source-plugins/` 目录，供分发使用；**插件不参与 fpk 打包**（本安装包始终不内置数据源，安装后从插件目录加载）。
- 新增 GitHub Actions 工作流：推送 `vX.Y.Z` tag 自动用官方 fnpack 在线编译并发布 `music-meta-web-<版本号>.fpk` 到 Releases。

### 说明
- 1.3.0 相对 1.2.1 的主要区别：图标更新、插件目录整理、仓库迁至 GitHub 并启用自动发布，功能代码与 1.2.1 相同。

## [1.2.1] - 2026-09-09

### 新增
- 应用图标更新为设计稿处理版（新图标）。

### 变更
- 打包流程固化：版本号每次递增、产物文件名带版本号、旧版 fpk 保留不删（本地脚本 `打包fpk.sh`）。

## [1.2.0] - 2026-09-02

### 变更
- 应用信息完善：maintainer=Tianxiaodudou、distributor=A鱼儿；应用介绍 desc 改为详细中文 HTML 版。
- 空闲自动退出默认关闭（原默认 30 分钟），改为配置页可选项。
- 总数页过滤加「正在过滤…」提示、结果缓存与写后自动失效。
- 「无歌词过滤→刮削歌词→用于此字段」交互修复（完整歌词不再内联进按钮，改用 song_id 取值）。
- 修复字段写入后未从过滤器消失的问题（写操作统一失效过滤缓存）。

## [1.1.0] - 2026-08-31

### 新增
- 多数据源插件化重构：QQ 音乐 / 网易云 / 酷狗 / 酷我 / LRC Lib / TheAudioDB / iTunes 单文件插件即插即用。
- 全局共享限速 + 本地缓存（SQLite）：同曲目零重复请求，缓存支持已确认歌曲一键导出。
- AcoustID 音频指纹识别模式（需自备 Key）。
- 目录级写保护、真实写入开关、中文错误提示。

## [1.0.0] - 2026-08-30

### 新增
- 首个可安装版本：按「歌曲名-歌手」文件名识别 → 多源检索 → 自动写入 / 人工辅助队列。
